<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Jobs</title>
  </head>
  <body>
    Jobs

    <% jobs.forEach(function(job) { %>
    <h2><%= job.title %></h2>
    <p><%= job.description %></p>
    <% }); %>
  </body>
</html>
